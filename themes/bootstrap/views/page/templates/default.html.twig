{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <article class="singlePage">
            <header class="page-header text-center">
                <h1>{{ page.title }}</h1>
            </header>

            <div class="text">
                {{ page.content | raw }}
            </div>
        </article>
    </div><!--/.container-->

{% endblock %}